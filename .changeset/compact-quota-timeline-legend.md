---
'agent-usage-all-in-one': patch
---

Drop the Provider colour legend from the quota timeline: every lane already carries its Agent name and colour, so the legend keeps only the current, elapsed, and upcoming window styles.
