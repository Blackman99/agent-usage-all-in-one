---
'agent-usage-all-in-one': patch
---

Remove persisted Demo Agent data automatically when the daemon starts without explicit demo mode, so ordinary development sessions cannot retain fixture providers from an earlier demo run.
