---
'agent-usage-all-in-one': patch
---

Redraw the quota timeline whenever its data, range, mode, or theme changes so every Agent lane appears once its quota loads instead of freezing on the first Provider rendered.
