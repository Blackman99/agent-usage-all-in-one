---
'agent-usage-all-in-one': patch
---

Swap each Agent's brand mark when the theme changes instead of keeping the first-painted one, and keep the dashboard alive when a Provider payload arrives without billing domains rather than throwing and freezing every later update.
