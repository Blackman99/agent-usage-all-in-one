---
'agent-usage-all-in-one': patch
---

Answer a manual refresh inside the Token and model cost view: the summary, analysis, and breakdown panels now show their edge progress line while the refresh request and the background collection it queues are still running, instead of leaving the header button as the only sign that anything happened.
